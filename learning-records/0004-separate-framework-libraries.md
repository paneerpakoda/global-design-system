# Frameworks require separate component libraries

The user correctly identified that React and Flutter cannot directly share the same component source code and therefore require separate framework libraries. Future teaching can build on the model of shared design intent and tokens with platform-specific component implementations.
