# Tokens are shared while component code is separate

The user correctly identified colour and typography tokens as shared inputs to React and Flutter components, while the component code itself remains framework-specific. This confirms the user can distinguish shared design decisions from platform implementation.
