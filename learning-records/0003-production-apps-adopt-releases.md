# Production applications adopt design-system releases

The user correctly identified that the production application—not the design-system repository—must update its dependency after a new design-system package is published. This establishes that publishing a library and releasing an application are separate delivery events.
